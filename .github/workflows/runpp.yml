name: runpp

on:
  workflow_dispatch:

jobs:
  runpp:
    runs-on: self-hosted

    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: run
        run: |
          /Applications/Microsoft\ PowerPoint.app/Contents/MacOS/Microsoft\ PowerPoint
